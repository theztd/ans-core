# Generated by ansible role core
{% for item in ansible_play_batch %}
{% set short_name = item.split('.') %}
{{ hostvars[item].internal_ip | default(hostvars[item].ansible_ssh_host) }}      {{ hostvars[item].fqdn | default(hostvars[item].ansible_fqdn )}} {{ item }} {{ short_name[0] }}
{% endfor %}
