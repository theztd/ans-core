# Generated by ansible role core
{% for item in groups['cluster'] %}
{% set short_name = item.split('.') %}
# {# set internal_name = (hostvars[item].internal_name) | ternary("", hostvars[item].internal_name) #}
{{ hostvars[item].internal_ip | default(hostvars[item].ansible_ssh_host) }}    {{ item }} {{ short_name[0] }} {{ hostvars[item].internal_name|default("") }}
{% endfor %}
