# Generated by ansible role core
{% for item in groups['cluster'] %}
{% set short_name = item.split('.') %}
{{ hostvars[item].internal_ip | default(hostvars[item].ansible_ssh_host) }}       {{ item }} {{ short_name[0] }}
{% endfor %}
